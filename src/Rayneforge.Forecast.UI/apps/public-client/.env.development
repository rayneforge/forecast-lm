# ─── Auth ─────────────────────────────────────────────────────────
# In development, bypass MSAL and use dev-header authentication.
VITE_AUTH_MODE=dev

# These are unused in dev mode but kept for reference:
# VITE_ENTRA_CLIENT_ID=<spa-client-id>
# VITE_ENTRA_AUTHORITY=https://<tenant>.ciamlogin.com/<tenant-id>

# ─── API ──────────────────────────────────────────────────────────
# Injected by Aspire AppHost at runtime; fallback for standalone dev.
# VITE_API_BASE_URL=http://localhost:5000
